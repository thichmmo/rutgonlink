# Admin domain API

Global domain listing shows owner, verification, link usage and moderation state. Disabling clears verification and blocks redirect resolution; enabling only removes the admin block, so DNS must be verified again.

Verification: disable a custom domain and confirm all short-link lookups on that host return 404.
