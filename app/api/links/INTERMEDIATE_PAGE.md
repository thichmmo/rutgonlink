# Intermediate-page API

Link create and update endpoints persist the opt-in video-style page and validate image values as HTTP(S) URLs or image data URLs within the 2 MB request limit.

Verification: `npx prisma validate`; `npx tsc --noEmit -p tsconfig.root-check.json --pretty false`.
