# Primary domain validation

Route tạo link lấy hostname chính từ `lib/site-config.ts` để chặn URL đích trỏ ngược về chính ứng dụng sau khi đổi domain.

Verification: URL thuộc domain chính bị từ chối; URL ngoài domain vẫn tạo được.
