# Intermediate-page dashboard wiring

The links dashboard carries `enableIntermediatePage` and `intermediateImage` into edit and duplicate flows so the setting is not lost when a link is managed.

Verification: `npx tsc --noEmit -p tsconfig.root-check.json --pretty false`.
