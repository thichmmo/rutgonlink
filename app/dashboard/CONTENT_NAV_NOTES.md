# Content navigation

The dashboard sidebar and mobile drawer expose popup and post management. Bottom tabs remain limited to primary destinations to avoid crowding narrow screens.

Verify with `npx eslint app/dashboard/DashboardNavLinks.tsx app/dashboard/MobileNav.tsx` and check both menu layouts.
