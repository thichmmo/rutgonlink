# Domain-neutral debug input

Placeholder URL dùng domain mẫu thay vì domain production để màn hình có thể tái sử dụng sau khi đổi `NEXTAUTH_URL`.

Verification: input vẫn chấp nhận URL HTTPS hợp lệ của domain đang triển khai.
