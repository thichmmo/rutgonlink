# Shared admin UI

`AdminUi.tsx` contains the small visual primitives shared by operational pages: badges, loading/empty states, pagination and an accessible modal shell. Business logic remains in each page.

Verification: scoped ESLint plus keyboard/button smoke checks on desktop and mobile widths.
