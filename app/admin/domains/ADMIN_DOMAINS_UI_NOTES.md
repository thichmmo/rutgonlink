# Admin domains UI

The domain table combines ownership, verification, moderation and link usage. Re-enable clearly warns that DNS verification is not restored automatically.

Verification: search by domain/email, filter states and exercise disable/enable with audit checks.
