# Domain-neutral log labels

Dashboard log dùng nhãn chức năng thay vì hostname hardcode; dữ liệu vẫn nhóm theo các key `rutgon` và `tienich` hiện có.

Verification: bộ lọc và summary tiếp tục đọc đúng từng key site.
