# Google registration

Trang đăng ký dùng cùng provider discovery và callback validation với trang đăng nhập. Google signup tạo hoặc dùng lại user theo email ở callback server.

Verification: khi thiếu credentials nút Google không hiện; khi cấu hình đủ, callback tạo session và chuyển về `/dashboard`.
