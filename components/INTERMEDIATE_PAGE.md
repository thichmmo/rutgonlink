# Video-style intermediate page controls

Create and edit modals expose the opt-in toggle, image URL/upload, and preview. Uploaded images are limited to 2 MB before being sent as a data URL.

Verification: `npx eslint components/CreateLinkModal.tsx components/EditLinkModal.tsx`.
