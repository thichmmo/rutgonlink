server {
    listen 80;
    server_name rutgonlink.site www.rutgonlink.site;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name rutgonlink.site www.rutgonlink.site;

    ssl_certificate /etc/letsencrypt/live/rutgonlink.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rutgonlink.site/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3939;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /_next/static {
        proxy_pass http://127.0.0.1:3939;
        add_header Cache-Control "public, immutable, max-age=31536000";
    }
}

# Catch-all for custom domains (via Cloudflare proxy)
server {
    listen 80 default_server;
    server_name _;

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3939;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /_next/static {
        proxy_pass http://127.0.0.1:3939;
        add_header Cache-Control "public, immutable, max-age=31536000";
    }
}
